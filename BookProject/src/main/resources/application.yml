spring:
  application:
    name: bookstore-api
  datasource:
    url: jdbc:postgresql://192.168.1.11:5432/bookstore
    username: bookstore_user
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

